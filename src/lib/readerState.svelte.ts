export const readerPrefs = $state({
    fontSize: 18,
    theme: 'dark',
    brightness: 1.0
});

export const readerStatus = $state({
    isLoading: false,
    showToc: false,
    showSettings: false
});